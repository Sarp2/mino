{
  "name": "mino",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun run --filter @mino/web dev",
    "build": "bun run --filter @mino/web build",
    "db:gen": "bun run --filter @mino/db generate",
    "db:push": "bun run --filter @mino/db push"
  },
  "packageManager": "bun@1.3.1"
}
